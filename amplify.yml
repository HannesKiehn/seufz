version: 1
env:
  variables:
    key: value
frontend:
  phases:
    preBuild:
      commands:
        - cd seufz
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    files:
      - "**/*"
    baseDirectory: build
  cache:
    paths:
      - node_modules/**/*
